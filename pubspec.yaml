name: podeo_assignment_one
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.1
  bloc: ^8.0.3
  hydrated_bloc: ^8.1.0
  fluttertoast: ^8.0.9
  math_expressions: ^2.3.0
  flutter_svg: ^1.0.3
  path_provider: ^2.0.9
  device_preview: ^1.0.0
  email_validator: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
     - assets/icons/
